# Multithreaded HTTP Worker

HTTP Worker with Background Isolates (Thread) support.

## Work Remaining
- Write Test Cases
- Add support for multiple threads. Currently, only one background thread is used.